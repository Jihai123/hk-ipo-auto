# Phase 4 交付说明（首页搜索体验优化）

## 完成内容

- 本轮仅完成 Phase 4：小程序首页搜索体验优化。未改后端、未改 `/api/mp/*` 契约、未改评分页和时间表页核心结构。
- 实现能力：
  1. 股票代码输入校验（仅数字、空值拦截、长度上限校验）；
  2. 不足 5 位自动左侧补零；
  3. 非法输入拦截并提示（toast）；
  4. 最近查询本地存储（去重 + 限长 + 最新优先）；
  5. 最近查询快捷入口点击即跳评分详情页；
  6. 输入提交与历史点击统一走评分页跳转路径。

## 最小变更范围

仅改动 4 个文件：

- `miniprogram/pages/home/index.js`
- `miniprogram/pages/home/index.wxml`
- `miniprogram/pages/home/index.wxss`
- `miniprogram/utils/search.js`

## 搜索历史存储策略

- Key：`hk_ipo_recent_searches_v1`
- 条数：最多 6 条
- 规则：
  - 仅保存 5 位数字代码；
  - 去重（重复查询置顶）；
  - 最新优先；
  - 存储读取失败时回退为空数组。

## 本轮未做内容

- 未新增后端接口。
- 未修改 `server.js`。
- 未实现模糊搜索、股票名联想、收藏、登录。
- 未新增独立搜索结果页。

