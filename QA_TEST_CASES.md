# 港股新股自动评分系统 v3.0 - QA 验收测试用例

> **测试范围**: 全部 API 端点 + 核心评分引擎 + 前端交互
> **测试环境**: Node.js >= 14, Express 服务端口 3010
> **测试数据**: `cache/02768.txt`（22KB）、`cache/02714.txt`（2.1MB）、`data/sponsors.json`、`data/ipo-sponsors.json`
> **日期**: 2026-02-08

---

## 一、功能性测试（Functional Tests）

### 1.1 API 健康检查 `/api/health`

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| F-001 | `GET /api/health` | 返回 `{ status: "ok", version: "3.0", sponsorsLoaded: N, timestamp: "..." }`，HTTP 200 | 检查 sponsorsLoaded 数量 > 0，timestamp 为合法 ISO 格式 | |
| F-002 | 服务启动后立即调用 `/api/health` | sponsorsLoaded 应 >= 244（FALLBACK 数据量） | 确认 sponsors 数据在启动时正确加载 | |

### 1.2 保荐人数据 `/api/sponsors`

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| F-003 | `GET /api/sponsors` | 返回 `{ count: N, source: "json"|"fallback", data: {...} }`，HTTP 200 | source 字段应为 "json"（存在 sponsors.json 时） | |
| F-004 | 删除 `data/sponsors.json` 后调用 `/api/sponsors` | source 应回退到 "fallback"，data 仍包含保荐人数据 | 验证 FALLBACK_SPONSORS 兜底机制正常 | |
| F-005 | 检查返回的保荐人数据结构 | 每个保荐人应包含 `rate`、`count`、`winRate` 等字段 | 抽检 3-5 个保荐人的数据完整性 | |

### 1.3 TOP 保荐人 `/api/sponsors/top`

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| F-006 | `GET /api/sponsors/top` | 返回默认 20 条记录，按 count 降序排列 | 验证排序正确性和去重逻辑 | |
| F-007 | `GET /api/sponsors/top?limit=5` | 返回 5 条记录 | 验证 limit 参数生效 | |
| F-008 | `GET /api/sponsors/top?limit=0` | 返回空数组 `[]` | 边界参数处理 | |
| F-009 | `GET /api/sponsors/top?limit=abc` | 回退到默认值 20 条记录 | `parseInt("abc")` 为 NaN，`|| 20` 兜底 | |
| F-010 | 检查返回数据中的保荐人 count 值 | 所有 count 应 >= 5（过滤条件） | 验证 `data.count >= 5` 筛选逻辑 | |

### 1.4 招股书搜索 `/api/search/:code`

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| F-011 | `GET /api/search/02768` | 返回 `{ success: true, results: [...] }`，results 非空 | 验证能从 HKEX 搜索到招股书 PDF 链接 | |
| F-012 | `GET /api/search/2768`（无前导零） | 同 F-011，`formatStockCode` 自动补零 | 检查 code 是否被格式化为 "02768" | |
| F-013 | `GET /api/search/99999`（不存在的代码） | 返回 `{ success: true, results: [] }` 或搜索异常 | 确认不会抛出未捕获异常 | |

### 1.5 主评分 API `/api/score/:code` - 使用缓存数据

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| F-014 | `GET /api/score/02768`（有缓存） | 返回 `{ success: true, totalScore: N, rating: "...", scores: {...}, elapsed: "..." }` | 验证走缓存路径，响应速度 < 2s | |
| F-015 | 检查 F-014 返回的 scores 结构 | 包含 `oldShares`、`sponsor`、`cornerstone`、`lockup`、`industry` 五个维度 | 每个维度有 score、reason、details 字段 | |
| F-016 | 验证 02768 的旧股检测 | score 应为 0（无旧股），reason 包含"无旧股"相关描述 | 对照 cache/02768.txt 内容验证准确性 | |
| F-017 | 验证 02768 的行业分类 | 应能识别出行业赛道并给出对应分数 | 检查 track 字段是否合理 | |
| F-018 | `GET /api/score/02714`（大文件缓存，2.1MB） | 正常返回评分结果，不超时 | 验证大文件处理能力，检查 elapsed 时间 | |
| F-019 | 验证 02714 的五维度评分 | 每个维度 score 在 [-2, +2] 范围内 | totalScore = 各维度 score 之和 | |
| F-020 | 验证 totalScore 与 rating 映射 | 6+="强烈推荐", 4-5="建议申购", 2-3="可以考虑", 0-1="谨慎申购", <0="不建议" | 对照 rating 值和 totalScore 的一致性 | |
| F-021 | `GET /api/score/2768`（无前导零） | 同 F-014，能正确找到缓存并评分 | `formatStockCode("2768")` → "02768" | |

### 1.6 评分引擎 - 五维度详细验证

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| F-022 | 旧股检测：包含"銷售股份"的文本 | score = -2，hasOldShares = true | 检查 evidence 中的上下文引用 | |
| F-023 | 旧股检测：仅有"新股份"的文本 | score = 0，hasOldShares = false | 确认不误判 | |
| F-024 | 保荐人识别：缓存 02768 中的保荐人 | 应能从文本或 ipo-sponsors.json 映射表中识别出保荐人 | 验证 sponsors[] 数组非空 | |
| F-025 | 保荐人评分：winRate >= 70% 的保荐人 | score = +2 | 对照 sponsors.json 中的 winRate 数据 | |
| F-026 | 保荐人评分：winRate 40%-70% 的保荐人 | score = 0 | 中等表现区间 | |
| F-027 | 保荐人评分：winRate < 40% 的保荐人 | score = -2 | 低表现保荐人 | |
| F-028 | 基石投资者：含"高瓴"、"红杉"等明星投资者 | score = +2，investors[] 列出识别到的投资者 | 检查匹配关键词列表的完整性 | |
| F-029 | 基石投资者：无基石投资者章节 | score = 0 | 确认不扣分 | |
| F-030 | Pre-IPO 禁售期：有 Pre-IPO 且有禁售条款 | score = 0 | 正常情况不加不减 | |
| F-031 | Pre-IPO 禁售期：有 Pre-IPO 但无禁售条款 | score = -2 | 风险警示 | |
| F-032 | Pre-IPO 禁售期：无 Pre-IPO 投资者 | score = 0 | 中性处理 | |
| F-033 | 行业 - 热门赛道（AI/芯片/机器人） | score = +2，track 显示匹配的赛道 | 验证 HOT_TRACKS 匹配 | |
| F-034 | 行业 - 成长赛道（医疗器械/新能源） | score = +1 | 验证 GROWTH_TRACKS 匹配 | |
| F-035 | 行业 - 低弹性赛道（食品/公用事业） | score = -1 | 验证 LOW_ELASTICITY_TRACKS 匹配 | |
| F-036 | 行业 - 规避赛道（物业/地产/金融科技） | score = -2 | 验证 AVOID_TRACKS 匹配 | |
| F-037 | 行业 - 无法分类 | score = 0，track 为空或"其他" | 中性处理 | |

### 1.7 缓存管理 `/api/cache/clear/:code`

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| F-038 | `GET /api/cache/clear/02768` | 返回 `{ success: true, message: "已清除 02768 的缓存" }` | 确认 cache/02768.txt 被删除 | |
| F-039 | 再次调用 `GET /api/cache/clear/02768` | 返回 `{ success: true, message: "02768 无缓存" }` | 重复清除幂等处理 | |
| F-040 | 清除后调用 `GET /api/score/02768` | 系统应重新下载并解析 PDF（走网络路径） | 验证缓存失效后的重建流程 | |

### 1.8 前端页面

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| F-041 | `GET /` 访问首页 | 返回 HTML 页面，HTTP 200 | 检查页面是否正常渲染 | |
| F-042 | 在前端输入框输入 "02768" 并提交 | 显示评分结果卡片，包含五维度详情 | 验证 UI 展示与 API 返回数据一致 | |
| F-043 | 在前端输入框输入 "2768" 并提交 | 同 F-042，前端或后端自动格式化 | 验证用户友好的输入处理 | |

---

## 二、边界条件测试（Boundary Tests）

### 2.1 股票代码边界

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| B-001 | `GET /api/score/00001`（最小五位数代码） | 正常处理（返回评分或"未找到招股书"） | formatStockCode 输出 "00001" | |
| B-002 | `GET /api/score/99999`（最大五位数代码） | 正常处理（返回"未找到招股书"） | 不存在的代码应优雅处理 | |
| B-003 | `GET /api/score/0`（单字符） | 格式化为 "00000"，返回搜索结果或错误 | formatStockCode("0") → "00000" | |
| B-004 | `GET /api/score/1`（单数字） | 格式化为 "00001" | 验证 padStart(5, '0') | |
| B-005 | `GET /api/score/123456`（六位数） | 格式化为 "123456"（padStart 不截断） | 检查超长代码的处理逻辑 | |
| B-006 | `GET /api/score/02768.HK`（带后缀） | `.HK` 被 `replace(/\D/g, '')` 过滤，结果为 "02768" | 验证非数字字符过滤 | |
| B-007 | `GET /api/score/HK02768`（带前缀） | 同 B-006，非数字字符被过滤 | 结果为 "02768" | |

### 2.2 缓存过期边界

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| B-008 | 将缓存文件 mtime 设为 6天23小时59分前 | 缓存命中（仍在 7 天以内） | `ageMs < 7*24*60*60*1000` | |
| B-009 | 将缓存文件 mtime 设为 7天0小时1分前 | 缓存过期，重新下载 | 边界刚过期 | |
| B-010 | 缓存文件存在但内容为空（0 bytes） | `readCache` 返回空字符串 → 后续评分流程应处理 | 检查空内容是否导致崩溃 | |

### 2.3 文本长度边界

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| B-011 | PDF 解析结果 < 5000 字符 | 抛出错误："PDF可能为扫描版，无法提取文字内容" | `text.length < 5000` 检测 | |
| B-012 | PDF 解析结果 = 5000 字符 | 通过验证，继续评分 | 边界值刚好通过 | |
| B-013 | PDF 解析结果 = 5001 字符 | 通过验证 | 边界值通过 | |
| B-014 | 超大文本（> 2MB，如 02714.txt） | 正常评分，不因截取 `slice(0, 25000)` 等丢失关键信息 | 各维度的文本截取窗口是否覆盖足够内容 | |

### 2.4 保荐人数据边界

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| B-015 | `GET /api/sponsors/top?limit=-1` | 返回空数组 `[]`（`slice(0, -1)` 会截取到倒数第1个） | 负数 limit 的行为 | |
| B-016 | `GET /api/sponsors/top?limit=99999` | 返回全部符合条件的保荐人 | 超大 limit 不报错 | |
| B-017 | 保荐人 count = 5（最小入围值） | 出现在 TOP 列表中 | `data.count >= 5` 包含边界 | |
| B-018 | 保荐人 count = 4 | 不出现在 TOP 列表中 | 被过滤 | |
| B-019 | 保荐人 winRate = 70.00%（评分边界） | score = +2（"≥70%" 包含 70） | 验证边界值判定 | |
| B-020 | 保荐人 winRate = 69.99% | score = 0（落入 40-70% 区间） | 验证边界下方 | |
| B-021 | 保荐人 winRate = 40.00% | score = 0（包含 40） | 验证中间区间边界 | |
| B-022 | 保荐人 winRate = 39.99% | score = -2 | 落入 <40% 区间 | |

---

## 三、异常与错误处理测试（Error Handling Tests）

### 3.1 网络异常

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| E-001 | 断开网络后调用 `GET /api/score/09980`（无缓存） | 返回 `{ success: false, error: "..." }` 包含网络相关错误信息 | 不应返回 HTTP 500 裸堆栈 | |
| E-002 | 模拟 HKEX 返回 403 Forbidden | 搜索失败，返回友好错误消息 | 检查 axios 错误是否被捕获 | |
| E-003 | 模拟 HKEX 返回 404 Not Found | 搜索结果为空或返回错误消息 | 不崩溃 | |
| E-004 | 模拟 PDF 下载超时（> 180s） | 抛出超时错误，返回 `{ success: false, error: "..." }` | 验证 180s timeout 设置 | |
| E-005 | 模拟 HKEX 返回空 HTML（200 但无内容） | 搜索结果为空数组 | cheerio 解析空页面不崩溃 | |
| E-006 | 模拟 DNS 解析失败 | 返回友好错误消息 | `ENOTFOUND` 错误捕获 | |
| E-007 | 模拟连接被重置（ECONNRESET） | 返回友好错误消息 | 网络层异常处理 | |

### 3.2 PDF 解析异常

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| E-008 | 下载的文件不是 PDF（如 HTML 错误页） | 抛出解析错误，不写入缓存 | pdfParse 或 pdftotext 失败处理 | |
| E-009 | 下载的 PDF 为扫描版（纯图片） | 返回"PDF可能为扫描版"错误 | text.length < 5000 检测 | |
| E-010 | 下载的 PDF 已加密/有密码保护 | 返回明确的错误消息 | pdf-parse 加密文件处理 | |
| E-011 | 下载的 PDF 损坏（部分字节截断） | 返回解析失败消息 | try-catch 捕获解析异常 | |
| E-012 | `pdftotext` 命令不存在时 | 回退到 `pdf-parse` npm 包 | 验证双重解析策略 | |
| E-013 | PDF 含大量 `[]` 符号（中文丢失） | 日志输出警告，但不中断评分 | `bracketCount > 100` 检测 | |

### 3.3 数据文件异常

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| E-014 | 删除 `data/sponsors.json` | 系统使用 FALLBACK_SPONSORS 继续工作 | `getAllSponsors()` 兜底机制 | |
| E-015 | `data/sponsors.json` 内容为非法 JSON | JSON.parse 失败，回退到 FALLBACK | 错误被捕获不影响服务 | |
| E-016 | 删除 `data/ipo-sponsors.json` | 保荐人识别仅依赖文本提取，不崩溃 | 映射表不可用时降级 | |
| E-017 | `data/ipo-sponsors.json` 为空对象 `{}` | 无映射数据，依赖文本识别 | 空对象处理 | |
| E-018 | `cache/` 目录不存在 | 启动时自动创建 `mkdir({ recursive: true })` | 验证目录自动创建 | |
| E-019 | `cache/` 目录无写权限 | 写缓存失败，但评分流程不应中断 | `writeCache` 异常处理 | |

### 3.4 API 路由异常

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| E-020 | `GET /api/score/`（缺少 code 参数） | 返回 404（Express 路由不匹配） | 不会进入评分逻辑 | |
| E-021 | `GET /api/nonexistent` | 返回 404 或默认 Express 错误 | 不暴露服务内部信息 | |
| E-022 | `POST /api/score/02768`（错误方法） | 返回 404 或 405（仅定义了 GET） | HTTP 方法检查 | |

---

## 四、安全输入与鲁棒性测试（Security & Robustness Tests）

### 4.1 注入攻击

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| S-001 | `GET /api/score/<script>alert(1)</script>` | 非数字字符被 `replace(/\D/g, '')` 过滤，结果为 "00001" 或空 | 无 XSS 风险，HTML 标签被过滤 | |
| S-002 | `GET /api/score/02768;rm -rf /` | 分号和其他字符被 `replace(/\D/g, '')` 过滤为 "02768" | 无命令注入风险 | |
| S-003 | `GET /api/score/02768' OR '1'='1` | 非数字被过滤，不影响系统 | 无 SQL 注入（系统仅在 pdftotext 调用和文件路径中用到 code） | |
| S-004 | `GET /api/score/..%2F..%2F..%2Fetc%2Fpasswd` | `formatStockCode` 将非数字全部去除 | 无路径遍历风险 | |
| S-005 | `GET /api/score/${require('child_process').exec('id')}` | 非数字被过滤，模板字符串不执行 | 无代码注入 | |
| S-006 | `GET /api/cache/clear/../../etc/passwd` | `formatStockCode` 过滤后变为纯数字，`getCachePath` 限定在 cache/ 目录 | 无路径遍历删除风险 | |
| S-007 | `GET /api/score/%00null%00byte` | 非数字被过滤为 "00000" | 空字节注入无效 | |

### 4.2 特殊字符输入

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| S-008 | `GET /api/score/０２７６８`（全角数字） | `replace(/\D/g, '')` 可能过滤全角数字 → 结果 "00000" | 验证全角数字是否被视为 `\D` | |
| S-009 | `GET /api/score/二七六八`（中文数字） | 非数字被过滤为 "00000" | 中文数字不被识别 | |
| S-010 | `GET /api/score/   02768   `（前后空格） | Express URL 解析会 trim，结果应为 "02768" | 检查 URL 编码空格处理 | |
| S-011 | `GET /api/score/02768%0d%0aInjected-Header:value` | CRLF 注入无效，非数字被过滤 | 验证 HTTP header 注入防护 | |
| S-012 | `GET /api/score/` 后跟 10000 字符的数字串 | `formatStockCode` 生成超长字符串 | 检查是否导致文件名过长或内存问题 | |

### 4.3 pdftotext 命令注入检查

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| S-013 | 检查 `parsePdfWithPdftotext` 函数的参数拼接 | `pdfPath` 应被双引号包裹，且来源为系统生成路径（`temp_{code}_{timestamp}.pdf`） | 无用户直接控制 pdfPath 的途径 | |
| S-014 | stockCode 含特殊 shell 字符（如 `$(id)`） | formatStockCode 过滤后仅含数字，临时文件名安全 | 验证 execSync 调用无注入 | |

### 4.4 CORS 与请求头

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| S-015 | 跨域请求 `Origin: https://evil.com` | CORS 中间件使用默认配置 `cors()`，允许所有来源 | 评估是否需要限制 Origin | |
| S-016 | 检查 API 响应头 | 不应暴露 `X-Powered-By: Express` 等敏感信息 | 建议添加 helmet 或移除该头 | |

---

## 五、性能/响应稳定性测试（Performance & Stability Tests）

### 5.1 响应时间

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| P-001 | `GET /api/health` | 响应时间 < 50ms | 纯内存操作，无 I/O | |
| P-002 | `GET /api/sponsors` | 响应时间 < 200ms | JSON 文件读取 + 序列化 | |
| P-003 | `GET /api/score/02768`（命中缓存，22KB） | 响应时间 < 2s | 缓存读取 + 评分计算 | |
| P-004 | `GET /api/score/02714`（命中缓存，2.1MB） | 响应时间 < 5s | 大文件评分处理 | |
| P-005 | `GET /api/score/09980`（无缓存，需下载） | 响应时间 < 30s | 包含搜索 + 下载 + 解析 + 评分 | |
| P-006 | `GET /api/sponsors/top` | 响应时间 < 100ms | 排序 + 过滤 | |

### 5.2 并发测试

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| P-007 | 同时发送 10 个 `GET /api/score/02768`（缓存命中） | 全部返回 200 且结果一致 | 并发读取缓存文件无冲突 | |
| P-008 | 同时发送 5 个不同代码的评分请求（均需网络） | 全部返回结果（成功或失败），服务不崩溃 | 并发外部请求的稳定性 | |
| P-009 | 同时发送 50 个 `/api/health` 请求 | 全部返回 200 | 基础并发能力 | |
| P-010 | 连续发送 100 次 `GET /api/score/02768` | 全部返回一致结果，无内存泄漏 | 检查 Node.js 进程内存（`process.memoryUsage()`） | |

### 5.3 连续请求与稳定性

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| P-011 | 每隔 1s 发送一次评分请求，持续 60s | 全部正常返回，无请求超时或失败 | 长时间稳定性 | |
| P-012 | 同一代码快速连续清缓存 + 评分 + 清缓存 + 评分 | 全部正确处理，无文件竞争条件 | 缓存读写的线程安全性 | |
| P-013 | 服务启动后连续运行 1 小时不重启 | 服务正常响应，无内存泄漏 | 监控 RSS 内存增长 | |

### 5.4 大文件与资源限制

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| P-014 | PDF 文件 > 100MB | 下载成功（maxContentLength = 150MB），但解析可能超时 | 检查内存占用和 OOM 风险 | |
| P-015 | PDF 页数 > 400 页 | pdfParse 设置 `max: 400` 限制 | 超出部分被截断但不报错 | |
| P-016 | 同时对 02714（2.1MB 缓存）执行评分 x 5 | 全部返回一致结果 | 大文件并发读取性能 | |

---

## 六、数据一致性与回归测试（Data Integrity & Regression Tests）

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| R-001 | 对同一股票代码连续评分 3 次 | 三次结果完全一致（deterministic） | totalScore、rating、各维度 score 不变 | |
| R-002 | 重启服务后对 02768 评分 | 结果与重启前一致 | 缓存持久化正确 | |
| R-003 | 对比 `scripts/test-scoring.js` 和主 `server.js` 的评分结果 | 逻辑一致时结果应相同 | 确认测试脚本未与主代码产生分叉 | |
| R-004 | 验证 `sponsors.json` 中所有保荐人的 winRate 计算 | `winRate = upCount / count * 100` | 抽检 5 个保荐人的数据一致性 | |
| R-005 | 验证 `ipo-sponsors.json` 中的股票代码格式 | 全部为 5 位数字字符串 | 不应有 "2768" 这样的 4 位代码 | |

---

## 七、爬虫模块测试（Crawler Tests - `scripts/crawl-sponsors.js`）

| 测试ID | 测试输入/操作 | 预期结果 | 实际观察点 | 是否通过 |
|--------|--------------|---------|-----------|---------|
| C-001 | `npm run crawl` | 成功执行，更新 `data/sponsors.json` 和 `data/ipo-sponsors.json` | 检查 updatedAt 时间戳更新 | |
| C-002 | 断网时运行 `npm run crawl` | 抛出错误并优雅退出，不覆盖已有数据 | 确认不会用空数据覆盖 | |
| C-003 | AAStocks 返回空页面 | 爬虫输出警告日志，不覆盖已有数据 | 空数据保护 | |
| C-004 | 检查爬虫输出的 JSON 结构 | sponsors.json 包含 name/count/upCount/downCount/avgFirstDay/winRate | 数据完整性 | |

---

## 测试执行摘要

| 测试类别 | 用例数 | 优先级 |
|---------|--------|-------|
| 一、功能性测试 | 43 | P0 - 必须全部通过 |
| 二、边界条件测试 | 22 | P0 - 必须全部通过 |
| 三、异常与错误处理测试 | 22 | P1 - 核心场景必须通过 |
| 四、安全输入与鲁棒性测试 | 16 | P1 - 安全相关必须通过 |
| 五、性能/响应稳定性测试 | 16 | P2 - 关注但允许优化 |
| 六、数据一致性与回归 | 5 | P0 - 必须全部通过 |
| 七、爬虫模块测试 | 4 | P1 - 上线前验证 |
| **合计** | **128** | |

---

## 已知风险与建议

1. **保荐人识别可靠性**: 已知部分 PDF 为扫描版导致保荐人提取失败（TEST_REPORT_02768.md），建议增加 OCR 降级方案
2. **无认证/限流**: 所有 API 无需认证且无速率限制，生产环境建议添加
3. **CORS 开放**: `cors()` 默认允许所有来源，建议配置白名单
4. **响应头暴露**: 默认暴露 `X-Powered-By: Express`，建议移除
5. **临时文件清理**: `temp_{code}_{timestamp}.pdf` 临时文件需确认解析后删除
6. **全角数字输入**: `formatStockCode` 使用 `\D` 正则可能过滤全角数字（"０１２３"），需确认是否为预期行为
